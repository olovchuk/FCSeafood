import { RefreshTokenGuestInterceptors } from './refresh-token-guest.interceptors';

describe('RefreshTokenGuestInterceptors', () => {
  it('should create an instance', () => {
    expect(new RefreshTokenGuestInterceptors()).toBeTruthy();
  });
});
